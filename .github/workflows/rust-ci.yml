on:
  push:
    branches:
      - main
    tags:
      - "*"
  pull_request:

name: CI
jobs:
  lint:
    name: Lint
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: "clippy, rustfmt"
      # make sure all code has been formatted with rustfmt
      - name: check rustfmt
        run: |
          rustup component add rustfmt
          cargo fmt -- --check --color always

      # run clippy to verify we have no warnings
      - run: cargo fetch
      - name: cargo clippy
        run: |
          rustup component add clippy
          cargo clippy --all-targets --all-features -- -D warnings

  test:
    name: Test
    strategy:
      matrix:
        os: [ubuntu-24.04, windows-2022, macos-14]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo fetch
      - name: cargo test build
        run: cargo build --tests
      - name: cargo test
        run: cargo test
      - name: cargo test build ansi
        run: cargo build --tests --features ansi_logs
      - name: cargo test ansi
        run: cargo test --features ansi_logs
      - name: cargo test (no default features)
        run: cargo test --no-default-features
      - name: cargo test otel-0-28
        run: cargo test --no-default-features --features otel-0-28
      - name: cargo test otel-0-29
        run: cargo test --no-default-features --features otel-0-29
      - name: cargo test otel-0-30
        run: cargo test --no-default-features --features otel-0-30
      - name: cargo test otel-0-31
        run: cargo test --no-default-features --features otel-0-31
      - name: cargo test otel-0-32
        run: cargo test --no-default-features --features otel-0-32

  deny-check:
    name: cargo-deny
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      - uses: EmbarkStudios/cargo-deny-action@v2

  publish-check:
    name: Publish Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo fetch
      - name: cargo publish check
        run: cargo publish --dry-run
